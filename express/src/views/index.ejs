<!DOCTYPE html>
<%- include('includes/head.ejs') %>
<body>
  <h1>
     
    <% if(typeof titulo !== 'undefined') { %>
        <%= titulo %>
    <%  } else { %>
      O titulo não foi enviado
    <% } %>
    <br>
    <%= titulo %><br>
    <%- titulo %>

    <% numeros.forEach(num => { %>
      <%= num %>
    <% }) %>
    <br>
    <br>
    <%= variavelLocal %>
  </h1>
<%- include('includes/footer') %>